# 📖 参考手册

CLI 命令、Slash 命令、环境变量、端口、工具签名的速查表。**不用通读，需要什么回来查就行。**

> [!tip] 最常用的三个命令
> ```bash
> hermes chat           # 开聊
> hermes gateway run    # 启动消息网关
> hermes doctor         # 出问题了先跑这个
> ```

---

## CLI 命令速查

### 核心命令

```bash
hermes chat           # 启动交互对话（默认 TUI）
hermes chat -m "..."  # 单次问答
hermes chat --no-tui  # 纯文本模式
```

### 配置

```bash
hermes init                     # 初始化配置
hermes config show              # 显示配置
hermes config validate          # 验证配置
hermes config edit              # 编辑配置
hermes doctor                   # 全面诊断
```

> [!tip] 改配置后记得 `hermes config validate`
> 语法错误能直接告诉你哪行有问题，比猜半天强。

### 会话

```bash
hermes sessions list            # 列会话
hermes sessions browse          # 浏览会话
hermes sessions show <id>       # 查看会话详情
hermes sessions export <file>   # 导出
hermes sessions prune           # 清理旧会话
hermes sessions delete <id>     # 删除
```

### 网关

```bash
hermes gateway run              # 启动
hermes gateway status           # 状态
hermes gateway stop             # 停止
hermes gateway restart          # 重启
```

### 技能

```bash
hermes skills list              # 列技能
hermes skills view <name>       # 查看
hermes skills create <name>     # 创建
hermes skills edit <name>       # 编辑
hermes skills delete <name>     # 删除
```

### 定时任务

```bash
hermes cron list                # 列任务
hermes cron run <id>            # 手动执行
hermes cron pause <id>          # 暂停
hermes cron resume <id>         # 恢复
hermes cron delete <id>         # 删除
```

### 检查点

```bash
hermes checkpoint save <name>   # 保存
hermes checkpoint list          # 列表
hermes checkpoint load <name>   # 加载
hermes checkpoint delete <name> # 删除
```

### 系统

```bash
hermes status                   # 系统状态
hermes version                  # 版本
hermes upgrade                  # 升级
hermes plugin list              # 插件列表
hermes plugin install <url>     # 安装插件
```

---

## Slash 命令（对话中）

在 TUI 中随时使用：

| 命令 | 功能 |
|------|------|
| `/help` | 显示帮助 |
| `/reset` | 重置当前会话 |
| `/undo` | 撤销上一步 |
| `/retry` | 重试上次回复 |
| `/save` | 保存检查点 |
| `/load` | 加载检查点 |
| `/model` | 切换模型 |
| `/provider` | 切换提供者 |
| `/tokens` | 显示 token 用量 |
| `/export` | 导出会话 |
| `/clear` | 清屏 |
| `/bye` | 退出 |
| `/fork` | 从当前会话创建分支 |
| `/summary` | 生成当前会话摘要 |

> [!tip] 最常用的 slash 命令
> `/model` 和 `/provider` 用来换模型调试、`/reset` 清上下文、`/tokens` 看看还有多少上下文窗口可用。

---

## 环境变量参考

所有环境变量通过 `~/.hermes/.env` 设置。

### 核心

| 变量 | 说明 |
|------|------|
| `HERMES_CONFIG` | 配置路径，默认 `~/.hermes/config.yaml` |
| `HERMES_HOME` | 数据目录，默认 `~/.hermes` |
| `HERMES_LOG_LEVEL` | 日志级别：debug/info/warn/error |

### API Server

| 变量 | 说明 |
|------|------|
| `API_SERVER_ENABLED` | 是否启用 API Server |
| `API_SERVER_KEY` | API Server 的认证密钥 |
| `GATEWAY_API_KEY` | 同上（兼容） |

### 提供者 API Key

```bash
OPENAI_API_KEY=***
ANTHROPIC_API_KEY=***
DEEPSEEK_API_KEY=***
GOOGLE_API_KEY=***
GROQ_API_KEY=***
OPENROUTER_API_KEY=***
TOGETHER_API_KEY=***
FIREWORKS_API_KEY=***
PERPLEXITY_API_KEY=***
MISTRAL_API_KEY=***
COHERE_API_KEY=***
XAI_API_KEY=***
AI21_API_KEY=***
CEREBRAS_API_KEY=***
AZURE_API_KEY=***
BEDROCK_ACCESS_KEY=...
BEDROCK_SECRET_KEY=***
```

> [!tip] 不用全配
> 只用你实际用到的提供者。其他不配也不会报错，Hermes 自动忽略。

### 消息平台

```bash
TELEGRAM_BOT_TOKEN=***
DISCORD_BOT_TOKEN=***
SLACK_BOT_TOKEN=***
WECHAT_APP_ID=...
WECHAT_APP_SECRET=***
FEISHU_APP_ID=...
FEISHU_APP_SECRET=***
WECOM_CORP_ID=...
WECOM_AGENT_ID=...
WECOM_SECRET=***
DINGTALK_APP_KEY=...
DINGTALK_APP_SECRET=***
```

### 代理

```bash
HTTP_PROXY=http://127.0.0.1:7890
HTTPS_PROXY=http://127.0.0.1:7890
NO_PROXY=localhost,127.0.0.1
```

### 其他

| 变量 | 说明 |
|------|------|
| `HERMES_DISABLE_ANALYTICS` | 禁用分析 |
| `HERMES_THEME` | TUI 主题 |
| `HERMES_NO_COLOR` | 禁用颜色 |
| `HERMES_DRY_RUN` | 只输出不执行 |

> [!tip] `.env` 比 `config.yaml` 优先级高
> 两个地方都配了同一个变量时，`.env` 赢。所以 API Key 放 `.env`，config.yaml 里不写敏感信息。

---

## 端口参考

| 端口 | 用途 |
|------|------|
| 8642 | API Server / Control UI |
| 11434 | Ollama |
| 8000 | vLLM |
| 30000 | SGLang |
| 8080 | llama.cpp |

---

## Tools Reference（完整工具签名）

### 终端工具

```python
terminal(command: str, timeout: int=180, background: bool=False, 
         workdir: str=None, pty: bool=False, 
         notify_on_complete: bool=False, watch_patterns: list=None)
```

### 文件工具

```python
read_file(path: str, offset: int=1, limit: int=500)
write_file(path: str, content: str)
search_files(pattern: str, target="content", path=".", 
             file_glob=None, limit=50)
patch(path: str, old_string: str, new_string: str, 
      replace_all: bool=False)
```

### 网络工具

```python
web_search(query: str)                   # 联网搜索
web_fetch(url: str)                      # 抓取网页内容
```

### 浏览器工具

```python
browser_navigate(url: str)               # 导航
browser_click(ref: str)                  # 点击
browser_type(ref: str, text: str)        # 输入
browser_snapshot(full: bool=False)       # 快照
browser_scroll(direction: str)           # 滚动
browser_press(key: str)                  # 按键
browser_vision(question: str)            # 视觉分析
browser_get_images()                     # 图片列表
browser_console(expression: str=None)    # 控制台
browser_back()                           # 返回
```

### 执行工具

```python
execute_code(code: str)                  # 运行 Python
delegate_task(goal: str, context: str,   # 委派任务
              toolsets: list=None)
```

### 调度工具

```python
cronjob(action: str, ...)               # 管理定时任务
```

### 记忆工具

```python
memory(action: str, target: str, content: str)
skill_view(name: str)
skill_manage(action: str, name: str, ...)
skills_list(category: str=None)
session_search(query: str=None)
```

### 视觉工具

```python
vision_analyze(image_url: str, question: str)
```

### 实用工具

```python
todo(todos: list=None, merge: bool=False)
process(action: str, session_id: str=None, ...)
```

---

## 相关笔记

- [[03-配置详解]] — 配置文件详情
- [[07-工具与技能系统]] — 工具系统详解
- [[09-提供者与模型]] — 提供者配置
